const { app, BrowserWindow } = require('electron')

/*
var jqr=require('jquery');

jqr("a[data-tooltip]").onmouseover (function(ev){
	var tt = $(this).attr('data-tooltip');
	jqr(".tooltip").html(tt);
});
*/
